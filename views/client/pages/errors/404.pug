doctype html
html(lang="vi")
  head
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title 404 Not Found
    style.
      body {
        margin: 0;
        height: 100vh;
        background: linear-gradient(135deg, #667eea, #764ba2);
        display: flex;
        align-items: center;
        justify-content: center;
        font-family: "Segoe UI", Roboto, sans-serif;
        color: #fff;
        text-align: center;
      }
      .container {
        max-width: 420px;
        padding: 2rem;
        background: rgba(255,255,255,0.1);
        border-radius: 16px;
        backdrop-filter: blur(6px);
        box-shadow: 0 10px 30px rgba(0,0,0,0.2);
      }
      h1 {
        font-size: 5rem;
        margin: 0 0 0.5rem 0;
        font-weight: 700;
        letter-spacing: 2px;
      }
      p {
        font-size: 1.2rem;
        margin-bottom: 2rem;
      }
      button {
        background: #fff;
        color: #764ba2;
        border: none;
        padding: 0.75rem 1.5rem;
        font-size: 1rem;
        border-radius: 30px;
        cursor: pointer;
        transition: all 0.3s ease;
      }
      button:hover {
        background: #f1f1f1;
        transform: translateY(-2px);
      }
  body
    .container
      h1 404
      p Trang bạn tìm không tồn tại hoặc đã bị di chuyển.
      button(onclick="history.back()") ← Quay lại
