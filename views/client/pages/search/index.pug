extends ../../layouts/default.pug 
include ../../mixins/box-head.pug
include ../../mixins/products-layout.pug
block main 
    .container.my-3 
        .row 
            .col-12 
                +box-head(titlePage)
        if(products.length > 0)
            +products-grid(products)
        else 
            .alert.alert-warning.text-center Không tìm thấy sản phẩm nào!